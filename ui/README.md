# IRS RAG UI (Vite + React)

## Setup

```bash
cd ui
npm install
npm run dev
```

- Dev server: http://localhost:5173 (proxies API to http://localhost:8000)
- Build: `npm run build`
- Preview build: `npm run preview`
